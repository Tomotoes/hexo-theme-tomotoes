<aside id="menu" <% if(theme.hideMenuDefault || (is_post() || page.layout === 'page') && theme.hideMenu){ %>class="hide"<% } %> >
  <div class="inner flex-row-vertical">
    <a href="javascript:;" class="header-icon waves-effect waves-circle waves-light" id="menu-off">
        <i class="icon icon-lg icon-close"></i>
    </a>
    <div class="brand-wrap" style="background-image:url(<%- url_for(theme.brand) %>)">
      <div class="brand">
        <a href="<%- url_for(theme.avatar_link) %>" class="avatar waves-effect waves-circle waves-light">
          <img src="<%- url_for(theme.avatar) %>" style="background-color: rgba(255,255,255,1)">
        </a>
        <hgroup class="introduce">
          <h5 class="nickname"><%- config.author %></h5>
          <a href="javascript:;" class="mail">少年锦时.</a>
        </hgroup>
      </div>
    </div>
    <div class="scroll-wrap flex-col">
      <ul class="nav">
      <div id="menuitems">
        <%
          var menuItem, isActive = function(item) {
              var pageUrl = url_for(page.path)
              return item.url === '/' ? pageUrl === url_for(item.url + 'index.html') : _.startsWith(pageUrl, url_for(item.url))
          };
              for (var i in theme.menu) {
                menuItem = theme.menu[i];
              %>
              <li class="waves-block waves-effect<% if(isActive(menuItem)){ %> active<% } %>">
                  <a href="<%- url_for(menuItem.url) %>" <% if(menuItem.target){ %>target="_blank"<% } %> >
                    <i class="icon icon-lg icon-<%= i %>"></i>
                    
                    <%=(menuItem.text || _.startCase(i)) %>
                    <% if(menuItem.num) {%>
                      <span id="<%- menuItem.num %>" class="digit"></span>
                    <% } %>
                  </a>
              </li>
            <% } %>
          </div>
      <strong id="followme">follow me</strong>
      <ul class="nav2" id="nav2icons">
          <%
            var menuItem, isActive = function(item) {
                var pageUrl = url_for(page.path)
                return item.url === '/' ? pageUrl === url_for(item.url + 'index.html') : _.startsWith(pageUrl, url_for(item.url))
            };
            for (var i in theme.menu2) {
              menuItem = theme.menu2[i];
            %>
              <li class="waves-block waves-effect<% if(isActive(menuItem)){ %> active<% } %>">
                <a href="<%- url_for(menuItem.url) %>" <% if(menuItem.target){ %>target="_blank"<% } %> <% if(menuItem.text){ %>title="<%-menuItem.text %>"<% } %> >
                  <i class="icon icon-lg icon-<%= i %> <%= i %>"></i>
                </a>
              </li>
          <% } %>
          
        </ul>

      </ul>

    </div>
  </div>
</aside>